# IMPORTANTE: use Python 3.11, 3.12 ou 3.13 para criar o venv.
# O Reflex 0.7.x não é compatível com Python 3.14.
reflex==0.7.14
pydantic>=2.7,<3
sqlmodel>=0.0.24,<0.1
fastapi>=0.111.0,<0.112.0
httpx>=0.27,<1.0  # cliente HTTP usado por xano_client.py para falar com o backend Xano

# Descomente a linha abaixo apenas se for conectar num SQL Server de verdade
# (ver instrução em rxconfig.py) em vez do arquivo SQLite local:
# pyodbc>=5.1.0
